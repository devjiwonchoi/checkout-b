{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "raycast-checkout-b",
  "title": "raycast-checkout-b",
  "description": "One click away from `git checkout -b <new-branch>`",
  "icon": "command-icon.png",
  "author": "devjiwonchoi",
  "categories": [
    "Developer Tools"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "index",
      "title": "checkout -b",
      "subtitle": "Generate a new branch name",
      "description": "One click away from `git checkout -b <new-branch>`",
      "mode": "view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.75.2"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^1.0.6",
    "@types/node": "20.8.10",
    "@types/react": "18.2.27",
    "eslint": "^8.51.0",
    "prettier": "^3.0.3",
    "typescript": "^5.2.2"
  },
  "scripts": {
    "build": "ray build -e dist",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "publish": "npx @raycast/api@latest publish"
  }
}